#!/bin/bash

echo "🧹 Очищення бази даних TOP BEAUTY"
echo "=================================="
echo ""
echo "Відкриваю інструмент очищення в браузері..."
echo ""

# Визначаємо шлях до HTML файлу
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
HTML_FILE="$SCRIPT_DIR/clear-db.html"

# Перевіряємо, чи існує файл
if [ ! -f "$HTML_FILE" ]; then
    echo "❌ Помилка: Файл clear-db.html не знайдено!"
    exit 1
fi

# Відкриваємо файл в браузері
if command -v xdg-open > /dev/null; then
    xdg-open "$HTML_FILE" 2>/dev/null
    echo "✅ Файл відкрито в браузері"
elif command -v open > /dev/null; then
    open "$HTML_FILE" 2>/dev/null
    echo "✅ Файл відкрито в браузері"
else
    echo "❌ Не вдалося автоматично відкрити браузер"
    echo "Відкрийте вручну: $HTML_FILE"
fi

echo ""
echo "📝 Інструкції:"
echo "1. У відкритому вікні браузера натисніть 'Очистити всю базу даних'"
echo "2. Підтвердіть дію"
echo "3. Закрийте вкладку та перезавантажте головну сторінку сайту"
echo ""

